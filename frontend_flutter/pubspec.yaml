name: makco_v2
description: Makco Metro Booking App v2.0
version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.5
  provider: ^6.0.3
  shared_preferences: ^2.0.15
  qr_flutter: ^4.0.0
  webview_flutter: ^4.2.2
  url_launcher: ^6.1.5
  google_fonts: ^6.0.0
  flutter_svg: ^1.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1

flutter:
  uses-material-design: true
  assets:
    - assets/icons/
